# Novelty — Theorem H (H-information-hierarchy)

Theorem H is a synthesis/unification theorem. Its individual parts (B2 collision,
G-info obstruction) are proved separately and are Gate-A established. The intended
contribution of H is an organizing **refinement preorder** over the observation maps.

**Gate-A BLOCKED (OB-32).** H's own increment — the incomparability of `O_finite` and
`O_theta` — is **NOT established**: under Theorem G's literal (constant) `O_theta`,
`O_theta ≺ O_finite` (comparable, not incomparable); only `O_finite ⋠ O_theta` holds. Genuine
incomparability needs a nonconstant `O_theta` + an exact reverse witness (absent). So H
currently contributes only the (correct, but modest) observation that its two established
obstructions live at different, comparable levels of a refinement preorder under
`O_oracle = 𝒵` (the full multiset). Not a standalone barrier; an organizing section at most.

**Novelty gate:** OPEN, and the incomparability increment is withdrawn pending redefinition.
The inherited obstructions (B2 for `O_finite`, G for `O_theta`) remain Gate-A established.
