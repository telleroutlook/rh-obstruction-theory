# Checker — Theorem J

No finite checker is used as proof. `lattice_index_instance_check.py` replays
the determinantal-divisor formula on one nontrivial integer matrix, checks the
accepted and rejected off-line coefficients, and verifies that mutating the
off-line vector changes the index. This is finite sanity evidence only.
